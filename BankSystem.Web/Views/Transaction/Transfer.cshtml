@model BankSystem.Web.Models.ViewModels.TransferViewModel

@{
    ViewData["Title"] = "Transfer Funds";
}

<div class="row justify-content-center mt-5">
    <div class="col-md-6">
        <div class="glass-card animate-fade-in p-5">
            <h3 class="mb-4"><i class="bi bi-send-fill text-primary me-2"></i> Transfer Funds</h3>
            
            <form asp-action="Transfer">
                <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>
                
                <div class="mb-4">
                    <label asp-for="FromAccountId" class="form-label">From Account</label>
                    <select asp-for="FromAccountId" class="form-control glass-input" asp-items="@(new SelectList(ViewBag.Accounts, "Id", "AccountNumber"))">
                        <option value="">-- Select Source Account --</option>
                    </select>
                    <span asp-validation-for="FromAccountId" class="text-danger"></span>
                </div>

                <div class="mb-4">
                    <label asp-for="ToAccountNumber" class="form-label">Destination Account Number</label>
                    <div class="input-group">
                        <span class="input-group-text glass-input border-end-0 bg-transparent"><i class="bi bi-person-badge"></i></span>
                        <input asp-for="ToAccountNumber" class="form-control glass-input border-start-0" placeholder="e.g. CHK1234567" />
                    </div>
                    <span asp-validation-for="ToAccountNumber" class="text-danger"></span>
                </div>
                
                <div class="mb-4">
                    <label asp-for="Amount" class="form-label">Amount ($)</label>
                    <div class="input-group">
                        <span class="input-group-text glass-input border-end-0 bg-transparent"><i class="bi bi-currency-dollar"></i></span>
                        <input asp-for="Amount" class="form-control glass-input border-start-0" type="number" step="0.01" />
                    </div>
                    <span asp-validation-for="Amount" class="text-danger"></span>
                </div>
                
                <div class="d-grid gap-2">
                    <button type="submit" class="btn btn-premium btn-lg">Send Funds</button>
                    <a asp-controller="Customer" asp-action="Dashboard" class="btn btn-glass mt-2">Cancel</a>
                </div>
            </form>
        </div>
    </div>
</div>
